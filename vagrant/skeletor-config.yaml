---
# Change - make 'xxx' unique across projects.
# And and the chosen ip for this project on at:
# https://madewithlove.atlassian.net/wiki/display/DT/Vagrant+boxes+hostname+list
ip: "192.168.56.100"

 # Change to your project name
projectname: "projectname"

ports:
    ssh: "56201"
    web: "8000"
    mysql: "33060"

# Set to true what you want to install on top of the base box
elasticsearch: false
mailcatcher: false
