#!/usr/bin/env bash
# This script holds some extra provision steps. These are temporary until they are baked into a new image, once it's worth it

echo "<< In project-extraprovision.sh we can add additional steps on top of the pre-baked image, if we want to"
